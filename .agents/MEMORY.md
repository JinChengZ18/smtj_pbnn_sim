# Agent Memory

> Append-only log of facts maintenance agents (Claude / Codex / others) need across sessions.
> Orientation + conventions live in [`README.md`](README.md); this file is the dated change log.

- 2026-06-26: Repository path migrated.
  - Old root: `D:\Documents\毕业设计-2026年5月10日\04PBNN仿真\smtj_pbnn_sim`
  - New root: `D:\Documents\Graduation Project-2026\04PBNNSim\smtj_pbnn_sim`
  - Maintenance agents should use the new English path only. The old Chinese path should be treated as stale and may no longer exist.
  - Claude/Codex maintenance metadata and linked-worktree pointers were migrated to the new root on 2026-06-26.
  - **Worktrees verified rewired**: `git worktree list` shows all 5 `.claude/worktrees/*` on the new path, **none `prunable`** — the post-move worktree follow-up is DONE (no `git worktree prune` needed).
  - **Stale CJK-path comments refreshed** to "historical" in `eda/hero/layout/run_drc.sh`, `eda/hero/layout/README.md`, `eda/hero/layout/gen_sa_layout.py`, and `eda/STATUS.md`. The ASCII ext4 build dir `/home/lenovo/smtj_eda_build` is STILL required, but now only for the `/tmp`-tmpfs idle-wipe reason (the `-rd input` UTF-8 mangling reason is gone).

- 2026-06-26: **Magic upgraded — PEX route unblocked.**
  - `/usr/local/bin/magic` rebuilt from source to **8.3.668** (>> required 8.3.306); stale apt 8.3.105 removed. sky130A techfile loads cleanly (`Using technology "sky130A", version 1.0.349`).
  - This unblocks the Magic/TCL **routing → LVS → PEX** route (was the only Magic-gated work). `eda/hero/layout/run_pex.sh` validates the `extract → ext2spice` toolchain (9 devices + parasitic C from the device-level GDS). `eda/MANUAL_SETUP_NEEDED.md §1` and `eda/STATUS.md` D8 updated to "done".
  - **LVS gotcha (confirmed):** the LVS netgen (Tim Edwards / open_pdks) is **NOT installed** — the only `netgen` on the distro is `/usr/bin/netgen`, the mesh generator (Schoeberl/Vienna). Build the right one before LVS; recipe in `eda/MANUAL_SETUP_NEEDED.md §3`.
  - WSL distro for all sky130 tooling = **`Ubuntu-24.04-EDA`** (ngspice-46 + OpenVAF-Reloaded + sky130A + KLayout + Magic 8.3.668).

- 2026-06-26: **Phase 0 toolchain gate met natively + Track A/B PEX results.**
  - LVS netgen (Tim Edwards) **1.5.321** built at `~/eda/netgen/bin/netgen` (apt `/usr/bin/netgen` is the mesh generator — don't use it for LVS). So the whole open EDA chain (ngspice/OpenVAF/sky130A/KLayout/Magic 8.3.668/netgen) now runs **natively** — IIC-OSIC-TOOLS Docker is optional, not the Phase-0 blocker the replan assumed.
  - **Track B (R3/R5)** `eda/extraction/writeline/`: Magic `extresist` validated (poly 47.96 vs 48.2 Ω/sq); column write-line round-trip metal R vs 776 Ω — N≤64 negligible, **N=256 met1/met2 = 16.5%** (IR 148 mV → pushes far cell below the 0.8958 V write point → p_sw shift), li1 catastrophic. Route write on met2+/widen/segment.
  - **Track A (R1/R2/R5)** SA layout device set fixed **9→11** (added Mp3/Mp4), DRC 0 violations, 11 MOSFETs + 35.25 fF extract; post-layout SA energy ~23–74 fJ/decision (5–15× the 5 fF read placeholder → R1); netgen LVS toolchain validated (device-level). **SA routing + full LVS = GUI last-mile** (`eda/hero/layout/LVS_GUI_CHECKLIST.md`).
  - **Step-executable plan** = `eda/PLAN_execution.md` (innovation main line A0/A1+A2/A3 broken into steps 1.1…3.5 with DoD + this session's status + "5 immediately-actionable" list).

- 2026-06-26: **3 top scriptable plan steps done (1.11/2.3/3.1) — errata R2/R4/R6 closed as design boundaries.**
  - 1.11 (`eda/hero/pareto_offset_cancellation.py`): plain StrongARM SA is Pareto-optimal at V_in≥0.5V/MNIST fan-in; auto-zero only earns its cost at V_in≤0.4V/wide fan-in → **R2 = quantified boundary** (budget offset vs V_T, not TMR).
  - 2.3 (`eda/testbenches/rc_isoenergy.py`): readout not free but efficiency-frontier; MC/Joule knee at moderate-res column-shared ADC b~5–6 (b=10 = 230× energy for 3.66× MC) → **R6 resolved**.
  - 3.1 (`eda/testbenches/run_write_driver.sh`): 1.8V sky130 driver into 776Ω → end-to-end ~1.61pJ = **2.05× the 0.783pJ Ohmic** at 0.9V delivery; needs regulated write rail → **R4 closed**.
  - Errata: E1/E2 done; R2/R4/R6 closed; R1/R3/R5 first-cut; R7 untouched. Commits FF'd to master through 65916ed. Work kept LOCAL (no push, per user). Remaining plan items need GUI (1.7) / installs (1.13 Xschem) / are larger (1.12, 2.4, 1.14).

- 2026-06-26: **iterated upstream modules before routing** (user: "routing is a finishing step, iterate the earlier modules first"). 1.12 write-DAC trim (`eda/hero/write_dac_trim.py`: 3–4 bits restore per-column offset to baseline, <1% write energy → Hero C2) + 2.4 grounded the RC ADC energy with the EXTRACTED 48fJ SA comparator (iterated `rc_isoenergy.py`; revised R6: readout dominates 88–99%, b-linear penalty gentle 38× not 230×, lever = amortize shared comparator). Commits e55d1ce, f6b35c7. **Hero C1+C2 + RC C3/R6 modeling complete. Routing 1.7 stays LAST (GUI). 2.5 (~38× recompute) needs the Ch5 canonical RC config — don't fabricate.**

- 2026-06-27: **plan critically re-validated + forward design + simulator made realistic + supplement rewritten journal-grade.** (1) Deliverables must be journal-grade: no thesis-chapter refs, no internal codes, no draft language ("honest"/"first-cut"/"hero") — supplement rewritten clean. (2) Old plan errors found (`eda/research/2026-06-27_plan_validation.md`): Kent arXiv:2509.13458 is STT-actuated tunable RTN (NOT tunable-barrier, does not preempt trinity); Nat.Electron -01458-3 is a commentary not measured silicon; C3 only partially novel (arXiv:2601.21807 already did low-res ADC); C1 genuinely novel (cite 2403.19374/2410.16915); venue JxCDC>TVLSI>TED>NCE>TCAS-I. (3) Forward design `eda/hero/ir_aware_writedac.py` (IR-aware per-row write pre-distortion); simulator reverse-supplemented with EDA data: reservoir_energy.py now has a grounded ADC (exp16 native ~30×), peripheral_energy.yaml read 5fJ→48fJ (write fraction 98.7%→93.8%). 24 tests pass. Commits 87d2779, d0a39a9.

- 2026-06-27: **2.5 done + academic supplement delivered.** Ch5 canonical RC config (N=100, ens=96, L=1000, dt=25ns) found in `experiments/16_rc_hardware_ppa.py`; `rc_energy_recompute.py` → RC-vs-ESN honest **~30–35×** (was 38×) after grounded ADC. **`article/supplement_eda_codesign.md`** = standalone academic增补 (clean, Chinese, paper main-text) synthesizing all EDA co-design results + 表S.1 shifted-claims + footnotes; 4 figs `article/figs/Supplement_local_01..04.png` via `eda/gen_supplement_figs.py` (headless matplotlib, article palette). **GUI-only figures remaining**: GDS layout render (KLayout xvfb) + Xschem schematic. E: ~498GB free → auto-install OK; matplotlib already present. Layout still deferred (supplementary). Commits 8733129, d1e650f (local, FF'd).

- 2026-06-27: **innovation circuits completed at sky130 level + old plan re-validated.** (1) Plan re-validation (`eda/research/2026-06-27_plan_validation.md`): Kent arXiv:2509.13458 mis-cited (RTN not tunable-barrier, doesn't preempt trinity); Nat.Electron -01458-3 is a commentary; C3 only partially novel (arXiv:2601.21807 prior); venue JxCDC>TVLSI>TED>NCE>TCAS-I. (2) Simulator made realistic from EDA data: `reservoir_energy.py` now bills the ADC (native 30×); `peripheral_energy.yaml` read=48fJ (write fraction 93.8%). (3) Innovation circuits: C1 readout front-end (`hero/run_readout_frontend.py`, R_TI=613Ω, in-circuit σ_pc≈2.5), C2/F1 write-DAC (`hero/run_write_dac.py` → resistor-string; `hero/ir_aware_writedac.py` IR pre-distortion), C3/F3 SAR (`testbenches/sar_capdac_energy.py`). Deliverable = `article/supplement_eda_codesign.md`. **Rule: deliverables journal-grade (see Claude memory deliverable-journal-grade): no chapter refs / internal codes / conversational hedging.** EN translation is LAST; layout/routing after designs frozen (now frozen). Commits through 2b9c747.

- 2026-06-26: **R7 + B8 closed, keystone σ firmed — ALL errata R1–R7 addressed.** R7/3.5 trinity barrier (`trinity_barrier.py`: ΔΔ=1.11=22.6%·E_b → VCMA ~0.56V; time-exclusive → constrained proposal). B8/3.4 adaptive-T (`adaptive_t.py`: ~50–57% fewer writes at iso-accuracy). **Keystone σ_offset FIRMED N=24→120: 9.21mV=0.39·V_T** (N=24's 11.05/0.47 was small-sample-high); propagated through the C1 chain + docs; strengthens "plain SA Pareto-optimal" (boundary now V_in≤0.29/0.34V). **Use 9.21mV/0.39·V_T going forward.** Errata: E1/E2 done; R2/R4/R6/R7 closed; R1/R3/R5 first-cut. Remaining gated: 1.7 routing (GUI), 1.13 Xschem (install), 1.14 figure, 2.5 (Ch5 config).

- 2026-06-26: **dual-model device policy (directive ②) implemented + validated.** Keep BOTH: behavioral (`eda/models/smtj_sot.va` + `gen_golden.py`) as the MAIN iteration model (cheap), and the LLG macrospin solver (`eda/vendor/vgsot-sim`, submodule, importable via `PYTHONPATH=eda/vendor/vgsot-sim/src`) as the compute-heavy VALIDATION. Bridge = `eda/testbenches/llg_validate.py`: drives vgsot-sim MC case `ser_sot_no_vcma_thermal` (**must pass `enable_self_heating=True`** = the calibration point; self-heating OFF sits ~120µA/100mV higher), maps V=|I_SOT|·R_SOT (R_SOT=776). **Result (UPDATED 2026-06-30 after FL-SOT fix + θ_SH=0.066 recal, 200 trials): LLG threshold 0.886V vs behavioral V_th 0.896V = ~9mV (0.40·V_T) match (PASS); the corrected LLG is now BROADER than the measured sigmoid (RMSE=0.16, R²<0 vs the monotonic sigmoid; was R²=0.92 with the bug).** The single-macrospin LLG (like Néel-Brown) underestimates the C2C-narrowed experimental slope (the η_c gap); the FL-SOT sign bug had cosmetically sharpened the curve and MASKED this. Thresholds agree; the slope gap is the expected sub-domain effect, not a calibration error. Above ~0.92V the LLG plateaus below the sigmoid (over-drive back-switching). chapter04.md:219/223 + fig 4.21 updated to match. vgsot-sim's `i_sot_list` for AP→P is NEGATIVE. Verilog-A transcription of vgsot-sim is a noted optional future path (heavy), not the main line.

- 2026-06-29: **figure production norms locked + analysis figs unified into the chapter decks.** Binding rules now live in [`eda/research/2026-06-28_figure_conventions.md`](eda/research/2026-06-28_figure_conventions.md) §"Project production norms": generators bake NO `(a)(b)(c)`/figure numbers (the deck adds letters, the markdown caption carries the number); NO hard-coded "Chapter…"/"图X.Y"/filename strings RENDERED into an image (docstring / `savefig(...)` paths are fine); NO Chinese in any deliverable figure; comparison plots label points DIRECTLY (no legend, de-collide overlaps, disambiguate two "ours" → "ours" vs "ours (auto-zero)"); in-figure math uses real subscripts (`$V_\mathrm{th}$` / `<tspan baseline-shift="sub">`), never "V_th". **Pipeline:** `eda/gen_supplement_figs.py` → letter-free panels in `figures/panels/`; `eda/build_ppt_figs.py` appends them to the hand-built `article/ppt/Chapter0{4,5}_local.pptx` (one deck per chapter — the separate `autofigs_*` decks were removed as fragmentation) and exports each appended slide **clipped to the panel row** → `article/figs/`. Hand-built slides untouched (deck backed up `.pptx.bak`, only own slides re-appended via a notes tag). Circuit schematics keep their vector `.svg/.pdf` (NOT deck-routed → no raster downgrade). Superseded standalone `gen_design_comparison_figs.py` + `figures/design_cmp_*.png` deleted (the comparison is now panel (c) of figs 16/18/5.9).
- 2026-06-29: Sibling project path migrated.
  - Project: `02MRAMSim`
  - Old root: `D:\Documents\毕业设计-2026年5月10日\02MRAM仿真`
  - New root: `D:\Documents\Graduation Project-2026\02MRAMSim`
  - Git repository root: `D:\Documents\Graduation Project-2026\02MRAMSim\vgsot-sim`
  - Maintenance agents should use the new English path only.
- 2026-06-29: Sibling project path migrated.
  - Project: `03ISim`
  - Old root: `D:\Documents\毕业设计-2026年5月10日\03伊辛模型`
  - New root: `D:\Documents\Graduation Project-2026\03ISim`
  - Git repository root: `D:\Documents\Graduation Project-2026\03ISim\isim_framework`
  - Maintenance agents should use the new English path only.
- 2026-06-29: Post-migration stale-cwd compatibility fixed.
  - This Codex thread still opened with old cwd `D:\Documents\毕业设计-2026年5月10日\04PBNN仿真\smtj_pbnn_sim`, which no longer existed and made shell startup fail before any command could run.
  - Added a Windows junction at that old cwd pointing to `D:\Documents\Graduation Project-2026\04PBNNSim\smtj_pbnn_sim`.
  - Treat the English path as canonical; the old CJK path is only a compatibility entry for stale tool/session metadata.

- 2026-07-01: **P3 Tier-1 done — comparator matrix now 5 designs; AppendixD figs renumbered.**
  - Same-flow offset-MC additions (N=120): `current_sampling` sigma/V_T=0.391 (input-pair-limited, sampling-mismatch cancellation observed); `dong_autozero` sigma/V_T=**0.167** (3.91 mV, ~58% below plain StrongARM — offset cancellation reproducibly works; StrongARM stays Pareto-optimal at the nominal point, AZ = validated conditional option). Netlists in `eda/hero/comparators/`, results `offset_mc_*.json`.
  - **AppendixD figure renumber**: schematics for the two new comparators inserted as `AppendixD_03/04`; old 03→05 (current-steering DAC), 04→06 (R-2R), 05→07 (SAR switching), 06→08 (Andrulis check). Generators + `postprocess_schematic_svg.py` GROUPS updated to match. Appendix D tables/prose + ch4 fig4.15(c) caption updated (5 designs); Dong/current-sampling rows migrated OUT of the qualitative D.5 readout table.

- 2026-07-01 (later): **SAR numbers REVISED + write-path IR-comp comparison added; appendix D tables renumbered.**
  - SAR cap-DAC re-run from the committed script SUPERSEDED the stale "representative" numbers (11–34% high): b8 conventional 394.7 fJ (total 779), monotonic **92.3 fJ** (total 476) → saving 4.3× not 3.25×. All dependents regenerated (comparison_results, cmp_* panels, AppendixD_07/08, fig 5.9, appendix D.4, ch5 caption); `gen_sar_switching_fig.py` now READS the JSON at runtime (no hardcoded energies). Do not reuse the old 457.4/140.6/841/525 anywhere.
  - Write-path IR-compensation reproduced (eda/design_survey/repro/): Truong per-row (parity with ours at the DAC quantization floor, delivery-model caveat in footnote) and Zhu global boost (P_sw residual floors ~0.10 vs our 0.006 → granularity is decisive). New **表D.3** in appendix D; SAR table → **表D.4**, qualitative tables → **表D.5/6/7**; Truong/Zhu rows migrated out of the qualitative matrix.
  - (addendum) **SAR/SS hybrid measured** (`eda/testbenches/sar_ss_hybrid.py` + summary JSON): worst-case strobing never beats pure SAR; average-case (6,2) wins only with ramp shared >=4 columns (438.8 vs 476.3 fJ at M=64). Last "hybrid_SAR_SS: not done" flag resolved (testbench JSON + aggregator caveat + appendix D.4 paragraph). All design-comparison reproduction work is now COMPLETE: every quantitative claim in appendix D regenerates from a committed script; qualitative matrix (表D.5/6/7) holds only infeasible-macro rows with cited per-cell justifications.

- 2026-07-01 (evening): **P4 complete — all remaining reproductions measured & integrated (appendix D final form).**
  - PICO-RAM comparator gating: measured 24–36% primitive saving (ensemble-dependent, NEGATIVE at ±2 V_T; no accuracy loss; coarse comparator must be auto-zeroed). RRAM flash-ADC slice: pooled 7-edge offset **0.392 V_T** (independent mismatch-wall confirmation); comparator-energy ratio vs SAR 2.27@b=3 → ~32×@b=8 (2^b−1 vs b); measured ladder-stiffness floor ~1.8 pJ/conv dominates. Yoon driver: VTC trim measured with our device model — 5 codes, mean step 88 mV = 3.75 V_T → state-CENTERING not probability-resolution (mV-class trim stays with the write DAC).
  - Appendix D additions: D.2 flash offset-wall sentence, D.3 Yoon paragraph, D.4 flash+gating paragraph (+scope footnotes); D.5 note extended. Scripts eda/design_survey/repro/{picoram_gating,rram_flash_slice,yoon_pbit_driver}.py; per-run scratch gitignored.
  - **The design-comparison programme is now closed end-to-end**: every one of the surveyed designs has either a same-flow measured point (11 designs) or a cited defensible qualitative row (the infeasible macros).
  - (addendum 2) **P4 circuits drawn**: new schematics AppendixD_07 (Yoon driver chain), 09 (flash-ADC slice), 10 (gating pair); SAR-switching fig renumbered 07→**08**, Andrulis 08→**11** (generators + postprocess GROUPS updated). Flash offset MC extended N=13→40 (pooled 0.418 V_T, first-13 seed-identical). Appendix D now: 11 figs, 7 tables, every compared circuit drawn + measured.

- 2026-07-02: **VA-model sync PARKED pending canonical vgsot-sim verification.** The canonical repo (`02MRAMSim/vgsot-sim`, branch `recal-and-article-sync`) is mid-flight with another stream's RTN-reservoir research landing (commits `a947912..21acff3` + ongoing fig/ppt working-tree churn). Per user: wait until that side's verification passes, THEN sync the Verilog-A model. Prep lives in [`eda/VA_SYNC_RUNBOOK.md`](eda/VA_SYNC_RUNBOOK.md); the parked 7-file `theta_SH` 0.04->0.066 doc de-stale is [`eda/pending_vgsot_destale.patch`](eda/pending_vgsot_destale.patch) (verified pure 27+/25- vs canonical HEAD `21acff3`; the live edits also sit uncommitted in the canonical tree). Still stale there (deferred, WIP-entangled): CLAIMS.md "#12 pending" rows + IMPLEMENTATION_STATUS "200 Oe +x" bias-field row. Background: the adversarial plan-completion audit (13 agents) rated the 877e4ec deliverable MET-WITH-MINOR-GAPS — both codebases green (core 33 tests; openvaf+ngspice cross-val max|dmz|=0.0079 < 0.02), the only gaps being this doc staleness.

- 2026-07-03: **All side branches consolidated into master.** `claude/bold-hertz-3ed513` merged for real (θ_SH=0.066 recal propagation: LLG cross-val 0.2mV→~9.4mV=0.40·V_T honesty fix in ch4 §fig4.21 + eda/testbenches regression + submodule bump to `877e4ec` + the parked VA-sync runbook/patch now under `.agents/eda/`); `claude/reverent-fermat-b38761` and `codex/chapter04-hardware-figure` recorded with `-s ours` merges (their content was already byte-identical in master history at `b0e95d1` / `ecd4111`). Post-merge de-stale: `eda/design_survey/README.md` dual-model row and PLAN_execution.md B1/3.6 rows updated to the 0.40·V_T result. Every local and remote branch is now ancestor-of-master; worktree branches (blissful-cannon, bold-hertz, festive-gauss, intelligent-perlman, modest-elion) can be pruned when their sessions close. VA-model sync itself remains PARKED (see 2026-07-02 entry).

- 2026-07-06: **计划文件审计 + 全量索引 + 审稿人视角统筹（24 智能体）+ `plans/` 落盘。**
  - **审计勘正**：errata「回填检查表」逐项对照正文核验后勾销（R1–R4/R6/R7 已回填；R5 以「Camsari 2019 综述 + 原型机边界」口径解决而非 6.95 pJ/bit 重定，图 4.13 的 4.2× 仍是器件级 0.78 vs per-update 5 pJ 混合口径——答辩前建议加口径限定）；PLAN_execution 1.13 实已完成（`eda/hero/schematics/` 14 个 `.sch`，图 4.15/5.8 由 Xschem 导出），1.14 改 ◑；「立即可开工 5 动作」中 2/3/4/5 已完成，仅 1.7 GUI 布线开口。README.md 新增 §1b 全量文件索引；STATUS.md 补 2026-07-06 快照。
  - **⚠️ 关键发现（已独立复现）**：**表 4.6 相对 master 代码陈旧**——重跑 `experiments/13_training_energy.py`（本地 run `runs/13_training_energy_20260706_225408`，runs/ gitignored）得 sMTJ **12.73 J**（表中 11.91）、sMTJ/STT-MRAM **1.22×**（正文 1.14×）、p-bit/sMTJ **3.89×**（头条 4.2×）：sky130 重地标落进代码后实验 13 未重跑回填。修正计划 = `plans/2026-07-06_defense_hardening.md` T3-1 步骤 0（最高优先级）。重跑顺带覆盖的 `figures/13a_*.png` 已 `git checkout` 还原，等表-图-文同步修订时一起重生。
  - **统筹产出**：工作流 run `wf_82532f22-886`（5 读者→5 审稿视角 38 提案→去重 12→逐条联网对抗核验→综合）；12 候选全部存活（创新类均 partially-preempted 带存活范围）。落盘 `plans/`：README（排程+unverified 清单）+ 答辩加固 5 项（T3-1 表4.6 审计、T3-2 EOT 攻击审计、T3-3 真 IPC 正交化、T3-4 V_th 慢漂移、T3-5 确定性重放列级共仿）+ 创新点 4 项（T1-1 免复位马尔可夫采样、T1-2 位翻转闭式认证界、T1-3 PBNN 原生 UQ、T1-4 电报储备池 ESP 认证）+ 拓展 3 项（T2-1 逐层 T 预算、T2-2 全温度自洽、T2-3 计数式 RC 读出）。排程：阶段 A=T3-1(0)→T3-2→T3-1→T3-3→T3-4；阶段 B=T1-1→T1-3/T2-1；阶段 C=期刊（T1-2/T1-4 主力）。
  - **附带勘误**：核验发现既有记录对 arXiv:2601.21807 的描述有误（实为集合平均读出去噪理论，非「2–4bit ADC 够用」）——PLAN_execution §0d 与 2026-06-27_plan_validation.md 的 C3 复核沿用前须读原文；引用前再核清单见 `plans/README.md` 末节。

- 2026-07-08 (later⁷): **B2/B3 实验完成（exp26 原生 UQ、exp27 逐层 T 预算）。** exp26：ECE 随 M 单调降 0.0165→0.0054（M≥4 优于 FP softmax 0.0098、远优 BNN 0.0313）、选择性预测 90% 覆盖 99.2–99.8%——零开销赢面；**诚实负结果**=OOD AUROC 随 M 反降（0.897→0.843）且不敌 FP 熵/BNN（θ 饱和检查点，平均化压掉认知分歧；D2D 注入预案留余步）。exp27：**负结果按预设降格**——逐层灵敏度 T≥2 全平坦、七种等能量调度全落 ±0.1 pp 噪声带、注水分类层无收益；封「资源分配」追问的消融证据。规范 run：26_native_uq_20260708_070501、27_layerwise_T_20260708_071156。

- 2026-07-08 (later⁶): **T1-1 核心完成（exp25 免复位采样）——三时序精度-能量前沿落地。** 单极性裸免复位结构性不可用（棘轮 π≥0.5，θ=0 处 π=0.978）；sign(θ) 极性+预畸变复原边缘但 T=4 方差膨胀 3.76×（实测 −0.7 pp）；**读后择极性 = 精确 i.i.d. + 省 46.9% 写路能量（0.831 vs 1.566 pJ/样本）**。电路级口径：读后择极性需写前逐单元读→per-MAC 诚实修正 +6%；复位时序 +90%；现行计费最接近读后择极性——psw_cond 闭环措辞基础。前置检查（同日）双绿灯：中段质量 fc1/fc2≈14%、双极性标定数据+fit_per_device_direction 现成。余步=8 种子稳健性+稿件段。

- 2026-07-08 (later⁵): **A6 实验部分完成（exp24 V_th 慢漂移）。** 每列漂移为约束模式（x\*=1.04 V_T ≈24 mV vs 每单元 2.39 V_T）；随机游走验证节拍公式 N\*=(x\*/σ_step)²；**BN 重校准对列漂移几乎无效、DAC trim 刷新有效且 ~70 pJ/次近零代价**（C2 trim 的失配+老化双重身份证据）。规范 run `runs/24_vth_drift_20260708_063805`；图 `figures/24_vth_drift.png`。稿件脚注等 ch4 落定。阶段 A 实验侧全部完成；剩余=A4/A5/A6 稿件半段（阻塞于用户并行编辑）+ 阶段 B。

- 2026-07-08 (later⁴): **A5 代码+图完成——规范 IPC 推翻半个旧叙事。** exp18 面板 (c)(d) 换正交 Legendre IPC（T=12000）：「升偏置释放二次容量」存活且机制更清晰（C2 0.01→15.7，奇对称破缺）；「总量大致守恒」不成立（C1 近不变、度≤3 窗口总量 30.8→44.1 随偏置增长=容量从高阶/长延迟窗口外搬入）；器件 vs 均值场 IPC 6.25 vs 30.5。图 5.6 已重生（复制改名路径）。**ch5 题注/[^ipc_proxy]/正文改写与 docx 等用户并行编辑落定后做**（chapter04.md 的 A4 整合同样在工作树待提交）。

- 2026-07-08 (later³): **A4 EOT 攻击审计完成（exp23）——负面预期未兑现，评测协议获正名。** 代码：FULL_STACK 输入梯度接通（`pbnn_linear/pbnn_conv`，采样 detach，97 测试过）、exp07 `_eval_pgd` 口径分离+EOT/重启/迁移。实测（规范 run `runs/23_eot_attack_20260708_053712`）：修正口径 53.2%（旧 52.12/52.5%）；EOT-PGD 不强于 CLT 替代攻击（CLT=解析期望=K→∞ EOT）；最强预算 45.4% > FP 白盒 43.4%；迁移不强于白盒；重训基线 BNN 54.4%/FP 43.4% → 差距 10–16pp 实例浮动、归因主要为二值化。稿件：表 4.4 PBNN 单元 53.22 + [^eot_audit] 脚注 + Athalye(arXiv:1802.00420)/Tramèr(arXiv:2002.08347) 尾注；指南 §7.10 记录图 4.8(h) 1.1pp 口径差。**chapter04.md 提交与 docx 押后**（用户并行编辑同文件中）。阶段 A 至此仅剩 A5 IPC、A6 漂移。

- 2026-07-08 (later²): **watcher 弃用 → docx 手动重生；用户三项决定落卷。** (1) **watcher 正式弃用**（用户指令）：新增 `scripts/build_docx.py`（pypandoc；折叠行内 `$$…$$`、剥图片 alt、`markdown-implicit_figures`；配方对已提交 chapter05.docx 复现校验——除 alt 元数据与 .md 后续润色外零差异）。chapter04.docx（表 4.6 修正）与 **chapter05.docx（发现落后于 fc6770b/12c7ffa 的两处去机翻润色）** 均已重生同步。watcher 相关表述已全面清理（全局 CLAUDE.md 用户偏好、本 README §2、PLAN_execution 横向、EXECUTION 规则、私有记忆 vgsot-dual）。(2) 龙卷风图**不收**正文/附录（用户定夺），留仓库级证据。(3) A7 [^hikstor_data] 数字用户已对 IEDM 全文自查确认，保留。runs/「已入库」指南措辞问题用户在独立会话处理（分支 claude/cool-bose-b4831a，保持 runs/ 不入库）。下一项：A4 EOT 攻击审计。

- 2026-07-08 (later): **执行队列建立 + 阶段 A 前三项落地（A1/A2/A3）。** 队列=`plans/EXECUTION.md`（合并两份计划的唯一执行顺序清单，live 状态）。(1) **A1 表 4.6 陈旧修正**：实际**两行**变动（sMTJ 7.90/12.73 J + stoch-ReRAM 448.40/453.22 J；复现指南 §7.1 原「仅 sMTJ 一行」diff 不完整已订正）；正文排名改「仅次于 STT-MRAM 与 FeRAM」、1.14×→1.22×、4.2×→3.9×；[^nv_ranking] 附旧占位值来历；图 4.13 经 deck 换图重导出（slide 13 blob 替换+纵横比修正+dpi419 裁剪，工作树其余 13 张用户重导出 PNG 未动）。(2) **A2**：ch4 标定器件 CD 改约 80 nm（第二章口径），无沟道工艺以约 100 nm 括注保留。(3) **A3 敏感性审计**：新增 `experiments/22_energy_sensitivity.py`（18 参数三档带宽、基线对规范 run 自校验、闭式名次反转点）→ 实测 p-bit/sMTJ 包络 1.5–10.9×（方向稳健）、sMTJ/STT 0.72–1.64×（跨 1 → 改口同档）；§4.5 增包络句 + [^energy_sens] 脚注（并列 6.4×/3.1× 口径，**errata R5 残余口径就此闭环**）。发现并挂芯片：复现指南「已入库 run」措辞与 runs/ 整体 gitignore 矛盾（task_27021e46）。龙卷风图暂为仓库级证据（figures/22_*），是否收正文待用户定夺。PPA 12 测试全过。

- 2026-07-08: **MTJ 版图/结构可行性论证完成 → `plans/2026-07-08_mtj_layout_structure.md`。**
  - **底线**：数据够做 L1 抽象 BEOL 集成版图（2T 单元 GDS + 黑盒，闭 PPA_grounding 精修项1、供 T3-5 单元模板）与 L2 结构物理研究（几何→保持Δ 自洽闭环、CD 设计窗+双CD双模候选、偶极串扰 pitch 界）；L3 可制造版图 = 生态不可行（开源 PDK 零 MRAM 模块；sky130B ReRAM 官方 DRC 五年未发布、仓库 2026-04 归档）+ sMTJ 堆叠专有，写成事实非自我设限。
  - **数据边界（核验划线）**：Hikstor EDL 2024（DOI 10.1109/LED.2024.3454609，全文已读）公开了 CD≈80nm、SOT track 200nm、**W 层 4nm/ρ=250µΩ·cm/θ_SH=−0.3**、R_SOT=776Ω（与标定值逐位同）；sMTJ 组（D_elec=65nm=RA/R_P 之商、t_FL/MgO/RA/R_P/Δ）公开域零出处、只能标「合作方晶圆实测标定」。
  - **口径修正 5 项**（详见计划文件）：ch4:233 「约100nm 归于第二章标定」误归属；[^hikstor_data] 数字须 IEDM 全文自查；ch2 应说明 D_elec 为派生量（跨仓库）；configs.py:42 「β-IrMn」注释陈旧（实为 β-W，Pai 2012 DOI 10.1063/1.4753947 已核）→ 并入 pending_vgsot_destale；θ_SH 材料值(−0.3) vs 器件有效值(0.066) 需一句区分。
  - **量级底牌（主会话实算，项目实参）**：邻居偶极耦合 2.1µm 间距 ≈2.7×10⁻⁵ kT（可忽略）、0.2µm ≈3×10⁻² kT（p≈0.5 处 ~1.5% 偏移）——串扰界研究有真实拐点，定位小节级（partially-preempted：Caçoilo arXiv:2312.05245 已做确定性 MTJ 版；sMTJ/p-bit 阵列版 unverified-absence）。

- 2026-07-03: **Global writing/workflow norms imported into project memory.** Source reviewed: `D:\Backups\Claude\CLAUDE-20260702.md` (UTF-8). Apply to this thesis project going forward:
  - Keep repo work on the canonical English path `D:\Documents\Graduation Project-2026\04PBNNSim\smtj_pbnn_sim`; run git from this subrepo root. Make meaningful local commits as checkpoints, but never push unless explicitly requested.
  - Use `.agents/MEMORY.md` for cross-agent memory and coordination; keep scratch plans, audits, and maintenance notes under `.agents/`, never inside user-facing manuscript folders.
  - For `article/*.md`, use one physical line per logical paragraph/list item except tables/code; preserve numbers, units, equations, footnote IDs, tables, image links, figure/table numbering, and code blocks during prose polishing.
  - Thesis prose standard: paragraph-level argument audit before sentence polish; remove AIGC fingerprints such as defensive meta-commentary, "本节/本章..." filler, dense "由此/据此/因此" scaffolding, gratuitous inline bold, and "其一/其二" enumeration unless it carries real argument structure. Keep the dense academic register; do not turn it conversational.
  - Footnotes are for explanatory notes; literature references belong in the end reference list. New or revised literature claims must carry DOI or arXiv links when available, and every citation/fact used in comparison figures or prose must be independently verifiable from the cited source or committed result files.
  - Figure norms remain binding: no baked-in figure numbers or panel letters in source figures, deliverable figures use English in-figure text, comparison points require directly verifiable values, and unverified literature data must be omitted or explicitly marked as unreported.
  - For factual fixes, edit the deliverable manuscript directly, but keep internal caveats and audit trails in `.agents/` rather than polluting `article/`.
