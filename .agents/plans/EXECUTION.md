# 执行队列（合并排程，live 状态；2026-07-08 建）

> **2026-07-08 循环执行收官**：阶段 A 六项全部完成（A1–A7，A4/A5/A6 的稿件段已随 49025bb 入正文）；阶段 B 四项核心完成（B1–B4）。**稿件整合已落**：ch4 得 [^vth_drift]/[^sample_timing]/[^layer_T]/[^eot_audit] 四脚注 + 表 4.4 修正口径单元；ch5 换正交 Legendre IPC 口径（题注/§5.2/[^ipc_proxy] 含订正记录）；两章 docx 同步，101 测试全绿。**剩余项均属答辩后/需用户定夺**：exp26 UQ 是否立小节（脚注承载不动）、exp25 三时序图是否入正文、8 种子稳健性尾巴、D2D-UQ 预案、CIFAR CNN 升级、T2-2/T2-3、T3-5、跨仓库口径修正（等 VA 同步窗口）；MTJ L1 已于 2026-07-08 完成（残留=可选 LVS/GDS 渲染图，见阶段 C 行）。

> 由 `README.md` 排程 + `2026-07-08_mtj_layout_structure.md` 前置口径修正合并而成的**唯一执行顺序清单**。每完成一项就地打勾并在 `../MEMORY.md` 记一行。规则：改 `article/*.md` 后运行 `python scripts/build_docx.py <chapter>` 手动重生 `.docx` 一起提交（watcher 已弃用）；数字改动必须以规范 run/入库脚本为准。

## 阶段 A（答辩前必做，当前窗口）

- [x] **A1 = T3-1 步骤 0**（2026-07-08 完成）：表 4.6 修正为规范 run 值——**两行**变动（sMTJ 7.90/12.73 J + stoch-ReRAM 448.40/453.22 J，指南 §7.1 原「仅 sMTJ 一行」的 diff 声明不完整已订正）；图 4.13 题注与 §4.5 正文（排名改「仅次于 STT-MRAM 与 FeRAM」、1.14×→1.22×、4.2×→3.9×）；[^nv_ranking] 重写并附旧占位值来历；图 4.13 经 deck 换图重导出（slide 13 blob 替换 + 纵横比修正 + dpi 419 裁剪）；guide/status.md 同步。规范 run 的 CSV 未入库（runs/ 整体 gitignored；guide 措辞问题已由用户在独立会话处理——保持不入库、改「本机留存」口径，分支 claude/cool-bose-b4831a 待合入）。docx 已于 2026-07-08 经 `scripts/build_docx.py` 手动重生同步（watcher 弃用；顺带发现并同步了 chapter05.docx 落后于 .md 润色的两处）。
- [x] **A2 = MTJ 计划口径修正 #1**（2026-07-08 完成）：chapter04.md「约100 nm」改归属——标定器件临界尺寸改为约 80 nm（第二章口径），无沟道工艺以「(临界尺寸约100 nm)」括注保留在对照句中。
- [x] **A3 = T3-1 步骤 1–3**（2026-07-08 完成）：`experiments/22_energy_sensitivity.py`（18 参数、三档带宽、基线对规范 run 自校验、闭式反转点）。实测：p-bit/sMTJ 包络 **1.5–10.9×**（方向全程不变，宽端来自 5 pJ 锚点自身 ±3×；统筹报告预跑的 2.7–4.8× 系未扫锚点本身，已被实测取代）；sMTJ/STT 包络 **0.72–1.64×** 跨 1（反转点：e_int8_mac ×2.03、stt.read ×1.64；对 FeRAM：×1.78/×1.49）；带内共 19 处名次反转（reversal_boundaries.csv）。§4.5 增包络句 + [^energy_sens] 脚注（并列 6.4×/3.1× 两口径，同时闭环 R5 残余）。**已定（2026-07-08 用户）**：龙卷风图不收进正文/附录，保持为仓库级证据 `figures/22_energy_sensitivity_tornado.png`（正文由包络句与 [^energy_sens] 脚注承载结论）；runs/ 下 CSV 保持不入库（脚本确定性可再生）。
- [x] **A4 = T3-2**（2026-07-08 完成；**结果远好于预期的负面**）：(1) 代码修复——`pbnn_linear/pbnn_conv` FULL_STACK 输入梯度接通（采样仍 detach，97 测试全过）、exp07 `_eval_pgd` 攻击/终评口径分离 + EOT(K)/多重启/迁移支持、PBNN 调用点终评改全栈 T=4；(2) 审计 `experiments/23_eot_attack_audit.py`（规范 run `runs/23_eot_attack_20260708_053712`）：旧口径复现 52.5%（对规范值 52.12% 浮动 0.4pp）、修正口径 53.2%；**EOT-PGD(K=10–20) 52.2–52.9% 不强于 CLT 替代攻击**（CLT 路径=随机前向解析期望=K→∞ EOT，原协议本就不属梯度混淆式宽松评测）；最强预算 (40步/K20/3重启) 45.4% 仍高于 FP 同预算白盒 43.4%；迁移攻击不强于白盒；重训基线实例 BNN 54.4%/FP 43.4% → 差距 10–16pp 随实例浮动、方向稳定，优势主要源于二值化（BNN 对照）。(3) 稿件整合：表 4.4 PBNN 单元 52.12→53.22（修正口径），¶归因句改写 + [^eot_audit] 审计脚注 + Athalye/Tramèr 尾注；指南 §7.10 记录图 4.8(h) 面板与新单元 1.1pp 口径差。**⏳ chapter04.md 的提交与 docx 重生押后**：用户正在并行会话编辑同文件（hikstor_data 脚注、§4.7 删减），等其落定后一并提交。
- [x] **A5 = T3-3**：真 IPC 正交化（余步已随 49025bb 入正文：图 5.6 题注/[^ipc_proxy]/§5.2 均为正交 Legendre 口径，2026-07-12 核验在场）。**代码与图已完成（2026-07-08）**：`metrics.py` 规范 IPC（合成验证精确：延迟线 5.000=rank、平方节点 1+1）+ `tests/test_ipc.py`；exp18 面板 (c)(d) 已换规范 IPC（T=12000、洗牌阈值、rank 上界打印；面板 (d) 改 C1/C2/C3/总量四曲线）；图 5.6 已重生（复制改名路径，非 deck）。**实测口径存亡**：「升偏置释放二次容量」**存活且更精确**（C2 = 0.01→9.0→15.7 随偏置单调升，机制=奇对称破缺；零偏置 C2≈0 为 tanh 奇函数的正确物理）；「总量大致守恒」**不成立**——C1 近乎不变（6.95→6.69），度≤3 窗口内总容量随偏置增长（30.8→44.1，rank 上界 120 未触及），诚实解读=偏置把容量从窗口外（高阶/长延迟）搬进可测窗口；C3 先降后回（23.8→17.5→21.7）；器件（ens=128）IPC 总量 6.25 vs 均值场 30.5（读出噪声压制高阶容量，比旧代理更凸显）。**余步（等 ch5 并行编辑落定）**：ch5:92 图 5.6 题注（「处理容量代理…保留 r²」→正交 Legendre IPC；(d) 描述改按度四曲线）、[^ipc_proxy] 脚注改写、§5.2/5.5 引用该结论处联动、docx 重生。
- [x] **A6 = T3-4**：V_th 慢漂移压力测试（余步已随 49025bb 入正文：[^vth_drift] 脚注含容忍包络+节拍公式+trim-vs-BN 对比，2026-07-12 核验在场）——**实验部分完成（2026-07-08，`experiments/24_vth_drift.py`，规范 run `runs/24_vth_drift_20260708_063805`）**。实测：静态漂移预算（−0.5 pp）**每列 x\*=1.04 V_T（约 24 mV）为约束模式** vs 每单元 2.39 V_T（T 采样+群体平均吸收单元级离散，与既有 per-column 敏感性证据一致）；随机游走验证节拍公式 N\*=(x\*/σ_step)²（σ_step=0.19 V_T/块 → 预测 N\*=30，实测 30 块附近触及预算线）；**策略对比的关键发现：BN 重校准几乎无效（漂移是每列 V_th 位置偏移、非人口统计漂移），DAC trim 刷新有效且近零代价（~70 pJ/次，2058 列 × e_dac_step）**——C2 trim 获得失配+老化双重身份的证据落地。图 `figures/24_vth_drift.png`。**余步（等 ch4 编辑落定）**：以脚注/小节入 §4.5 或 4.7 限制段（漂移容忍包络 + 节拍公式 + trim-vs-BN 对比一句），换锚 arXiv:2403.11988 + Nat.Commun 2024（DOI 10.1038/s41467-024-48152-0），与 PRApplied 23,054073 器件级反馈及 arXiv:2410.16915 权重空间补偿显式区分。
- [x] **A7 = MTJ 口径修正 #2**（2026-07-08 用户核实）：[^hikstor_data] 数字已由用户对 IEDM 2024 原文全文自查确认，脚注保留原数字，无需换锚。

## 阶段 B（答辩前争取）

- [◑] **B1 = T1-1**：免复位马尔可夫采样。**前置检查完成（2026-07-08）——双绿灯，全量投入**：(1) 中段质量充足：fc1/fc2 的 p∈[0.2,0.8] 占 14.7%/13.7%（约 26 万单元）、fc3 占 32.1%，饱和(<1%或>99%)仅 ~57%——θ×100 退化顾虑排除；(2) 双极性标定可用：`data/smtj_psw_curves/measured_0p75ns.csv` 含 A/B 两器件 × AP→P/P→AP 两方向（16+12+9+9=46 点），`calibration.py:113 fit_per_device_direction` 现成——读后择极性校正可从入库数据标定。**核心完成（2026-07-08，`experiments/25_resetfree_sampling.py`，规范 run `runs/25_resetfree_*`）**：(1) 链分析——τ(0V)=67.8 ns 由 NB 参数自洽复现；**单极性裸免复位结构性不可用**（脉冲棘轮：θ=0 处 π=0.978、且 π≥0.5 恒成立→负权重半平面不可达）；sign(θ) 静态极性+预畸变可复原边缘分布但中段单元工作在脉冲概率 ~0.0025、自相关 ρ≈0.95、T=4 方差膨胀 3.76×；(2) 精度实测（n=2000）——复位 i.i.d. 96.60%、免复位预畸变 95.90%（−0.7 pp，相关性代价实测）、**读后择极性 96.45%（i.i.d. 精确复原，差在采样噪声内）**；(3) 能量——复位 1.566 pJ vs 读后择极性 0.831 pJ（**省 46.9% 且零精度代价**）vs 免复位 0.783 pJ。**电路级口径要点**：读后择极性需写前逐单元读（popcount 读为列和、不供单元态），对现行 per-MAC 计费的诚实修正=+1 读（≈+6%）；复位时序则 +0.783 pJ（≈+90%）——现行「每样本一写一读」计费最接近读后择极性时序，psw_cond 脚注闭环措辞以此为基。**余步收口**：8 种子 D2D 稳健性 ✅ 2026-07-12（`experiments/25b_resetfree_d2d.py`，run `runs/25b_resetfree_d2d_20260712_221430`：读后择极性 96.47%±0.22 vs 复位基线 96.31%±0.22（同场 D2D 写误差），配对差 +0.16±0.34 pp 统计不可分辨——**校正的 46.9% 省写能优势在标定 CV=7.7% 的 D2D 失配下无精度劣化**；共模 V_th 假设、方向不对称超出一阶口径已注明）；稿件段核心已入正文（[^sample_timing]）；三时序前沿图是否入正文待用户定夺。
- [◑] **B2 = T1-3**：PBNN 原生 UQ——**核心完成（2026-07-08，`experiments/26_native_uq.py`，规范 run `runs/26_native_uq_20260708_070501`）**。M∈{2..64} 单样本前向集成（uncertainty.py 首次消费）+ exp23 基线实例 + sky130 口径能量轴。**赢面**：ECE 随 M 单调改善 0.0165→0.0054，**M≥4 优于 FP softmax（0.0098）**、远优 BNN（0.0313）；同批样本选择性预测 90% 覆盖 99.2–99.8%。**诚实负结果**：OOD AUROC 随 M 下降（MI 0.897→0.843、PE 0.920→0.855），θ 饱和检查点上不敌 FP 熵（0.905）与 BNN（0.950）——平均化压掉认知分歧，「域内信号平淡」风险以此形态兑现。**余步（部分收口 2026-07-12）**：D2D 注入预案 ✅（`experiments/26b_uq_d2d.py`，run `runs/26b_uq_d2d_20260712_221441`：每成员重抽标定 CV=7.7% 器件场的 M 集成，AUROC(MI) 0.857→0.838 与 nominal 0.867→0.835 全程重合、同样随 M 下降——**器件实例集成不复活 OOD 信号**，θ 饱和下 D2D 的 logit 扰动太小、与 exp08 的前向不敏感一致；诚实负结果，ECE/精度亦与 nominal 持平）；仍开：CIFAR-10 预案（OOD 复活的另一路线）、非理想性 UQ 退化谱、conformal×SPRT 联动、稿件段（定位=学位论文贡献点，「首个」类措辞零使用，按工程账对比 Liu 2022/SpinDrop）。
- [x] **B3 = T2-1**：逐层采样预算分配——**实验完成（2026-07-08，`experiments/27_layerwise_T.py`，规范 run `runs/27_layerwise_T_20260708_071156`），负结果按预设降格**。`schedules.py` 首次消费（constant_T/layer_depth_T）；逐层灵敏度：三层在 T≥2 后全部平坦（仅 T=1 掉 0.2–0.4 pp）；七种等能量调度（含注水填充廉价分类层至 T3=184）全落 97.45–97.56%（±0.04–0.09 pp 噪声带，3 次采样重复）——**与均匀调度不可分辨**；免费 L3 提升（+0.8% 能量、T3=64）亦仅 +0.11 pp。结论=MNIST-MLP 上 T 边际效用在 T≈2–4 饱和、逐层再分配无收益；作为消融证据封住「资源分配」追问。CIFAR-10 CNN 升级（T=16–32 区间）留作低优先余步；稿件段=一条消融脚注（等 ch4 落定）。
- [x] **B4 = MTJ L2a/L2c**（2026-07-08 完成，`eda/testbenches/structure_consistency.py` + summary JSON + `figures/structure_consistency.png`；稿件段余步**须含 MTJ 计划口径修正 #5**：θ_SH 材料值 −0.3（EDL 公开/Pai 2012）vs 器件级有效值 0.066 的一句区分；可选残留 LLG 核对 ✅ 2026-07-12（`eda/testbenches/dpdb_llg_check.py`，±10 Oe×1500 trials 自热 ON：写域实测 dP_sw/dB_z=+0.030±0.018/mT——比证书平稳基 0.275/mT 小约 9×（证书=最坏情形、对写域超覆盖一个量级），与补偿修正 Hk=140 kA/m 的脉冲域解析 0.054/mT 在 ~1.3σ 内一致、而 FMR 基线 Hk=50 kA/m 高估 5×——L2a 的 81% 补偿物理在场响应通道独立复现，双模型锚兑现）。**L2a 闭环**：几何预测保持 Δ=48.5 kT vs 公开平台带 59.3–64（同量级；差距有物理解释——K_eff=55.1 kJ/m³ 为界面 290.9 与形状 235.9 两大项之差、**81.1% 补偿**，+1% Ki → +5.3% Δ 的近补偿放大使 ~20% 差距仅相当于 ~4% 叠层参数精度，且定量解释 D2D CV(Δ)=7.7% 只需 ~1.4% 微观离散）；保持 Δ 与 NB 开关 Δ=4.91 两个量在输出中显式分列（守 parameter_validation.md:24 口径）。**设计窗**：同叠层达 Δ≈4.9 需 D_elec→17.2 nm 或 Ki 修剪 10%——**L2b 双 CD 候选据此修正**（非早前按面积线性猜的 ~70 nm；双 CD 方案需 17 nm 级光刻，吸引力下降，sMTJ 更可能是近补偿叠层变体）。**L2c 串扰界**：dp/dB=0.275/mT（2.19/Oe）；2T 间距 δp≈1.0e-5（黑盒磁学自洽的定量证书）；1% 偏移临界间距 **211 nm**（仅无 FET 密集 BEOL 域相关；含 ×1.5 点阵和裕量）。稿件段 ✅ 2026-07-12 已入 §4.6（版图/结构两段 + 图4.22/4.23 + [^drc_control] 试错脚注 + θ_SH 口径句 + 4 条新尾注 EDL/Pai/ASP-DAC/arXiv:2312.05245；方法学段「绝对面积仍俟版图细化」已按实绘版图更新）。

## 阶段 C（答辩后/期刊）

- [◑] **T1-2 位翻转闭式认证界**（2026-07-13 核心完成，稿件段押后）：**理论** = BSC 收缩等价定理（i.i.d. 翻转 ⇔ 全网潜概率收缩 q'=(1−2p)q+p，受损网络仍在模型类内——位置编码 INT8 无此性质，是 44pp 鲁棒性差距的结构根源）+ 末层三值边际的 min(Bernstein, 高斯+Berry–Esseen 0.56) 严格尾界 + 类并集 + 对 h 全期望。**实现** = `nn/certify.py`（闭式库）+ `tests/test_certify.py`（对暴力 MC 的可靠性测试 5/5 过）+ `experiments/28_bitflip_certification.py`。**实测（T=8/64，n=4000）**：(1) 等价性双实现全程一致（flip vs contracted 差在二项噪声内）；(2) **认证下界紧贴经验曲线**（修正版最终数，run `runs/28_bitflip_cert_20260713_002217`：T=64 全网格认证-经验差 ≤1.3pp——p=0.30 认证 0.531 vs 经验 0.543；T=8 至 p=0.15 差 ≤1.5pp、p=0.2 为 3.5pp）；(3) BER 反解：1pp 认证退化预算 = T8 7.9% / T64 9.0% 翻转率，0.5pp 档受 ±0.3pp 评估噪声支配须注明。**对抗核查（2 怀疑者，0 major）**：BE 常数 0.56=Shevtsova 2010（DOI 10.1134/S1064562410060062）核验通过、三值推导/偏置代数/并集/全期望分解全过；已修 5 条 minor（零方差退化角点补 t>0 门、CI 改单侧 t(7) 下置信沿、exp09 口径注释、try/finally、NaN-safe JSON）+ 经验曲线加二项误差棒（认证界约束总体精度、可合法高于单次经验点——图注须声明）。**稿件段（含淬火/退火信道口径声明 + BN 冻结口径 + 定位对照三线 prior art）等 ch4 编辑窗口**。
- [◑] **T1-4 电报储备池 ESP 认证**（2026-07-13 核心完成，稿件段押后）：**判据三层结构**（`experiments/29_esp_certification.py`，run `runs/29_esp_cert_*`）——(1) 逐行器件分辨充分界（严格 ∞-范数，每节点用自己的 Δ_i 与行范数）：仅认证 ρ≲0.06（平面 2%）——**结构性弱有物理解释**：慢器件（高 Δ）同时是陡器件（m′=Δ/V_c0），斜率通道里 (1−λ) 对消 → ρ_cert≈V_c0/(Δ_i·R_i) 与 dt 无关；(2) 线性化谱判据（沿驱动轨迹的雅可比谱半径）：边界立在 ρ≈1（教科书 leaky-ESN 值在器件映射下保持——零偏下 λ+(1−λ)ρ<1 ⇔ ρ<1 与 dt 无关，泄漏-斜率精确对消），随偏置右弯到 ρ≈1.7@100mV=**偏置致稳的量化**；1681 格点仅 3 点乐观；(3) 速率型孪生实证（Δ 异质性感知——最慢节点 τ≈0.8µs，绝对阈值判据会误判）：97% 平面收缩，非收缩区=高 ρ 低偏置的**双稳区**（对向饱和吸引子），偏置破缺对称性恢复收敛。**可靠性 0 违例**（认证区⊂收缩区）。**随机模式**（共同噪声孪生、读出层度量、显式声明非「无条件随机 ESP」）：ρ=1.4/1.8 零偏读出距离 2.0（对向饱和）→60mV 压到 0.25/0.20，与均值场双稳区吻合。**工作点判定**：默认 (0.9,0,8ns) 速率 −0.011/步、lin 0.995（贴边界，稿件注明裕量）；exp16 (0.5,0,25ns) 速率 −0.034/步、lin 0.968——第五章「保持 echo-state 区」的宣称就此有量化判据支撑。**对抗核查完成（2026-07-13，1 怀疑者，2 major + 5 minor 全修）**：核心数学经有限差分验证（雅可比相对误差 3e-9）；修复=速率地板伪影（孪生精确合流时 1e-300 地板扭曲速率场、潜在把深度收敛误判为不收缩→改「最后正距离点斜率+哨兵速率」）、文档描述的是旧版均匀界（重写为实际的逐行∞-范数可达区间判据、删死变量）、认证函数改偏置依赖的可达区间上确界（更紧且红线不再「构造性垂直」）、可靠性检查扩到双平面并入库 phase_dt.csv、随机同步改「对去相关基线的尾均值比较」（低 ρ 稳健同步比值 0.11–0.23；高 ρ 低偏置=吸引子锁定域）。**残留**：随机子网格单实现敏感（ρ=1.8 零偏两次运行分落同/异盆地）→稿件前须多种子；**稿件措辞三条硬约束**（核查开列）：线性化谱判据只能作指标（严格支配矩阵给 1.237>1、默认点裕量 0.5% 在方法噪声内）、孪生收缩区=非扩张必要条件证据而非 ESP 区（ρ=1.8 反例在案）、ch5「使储备池稳定工作在回声状态区」须以实证+显式保守证书双支撑表述。稿件段等 ch5 窗口（引 Jaeger/Ehlers 2025/PRE 110,024207 定位）。
- [x] **T2-2 全温度自洽**（2026-07-13 完成，含对抗核查修正重跑）：`thermal_scaling.py` 模块（Bloch/CC→81%补偿K_eff→Δ(T)/V_c0(T)/V_th漂移/V_T(T)/TMR(T)，实现预设 trim_route/shrink_route + n带假设全部显式声明）+ exp30 + LLG 双模型锚（I_50 比值 1.076/0.923 vs 链 1.073/0.926，200 trial 下~1% MC 不确定度内一致；方向由共享定律构造性保证，约束的是幅度）。**头条**：as-built n=2.18 无重标定 1pp 温窗 [292.8, 308.8]K±1K；实现×n 全表 [279,327]（n=1.8）至 trim/n=2.8 亚开级（b_crit 连续反演，经 shrink 实测验证；trim 实测受 5K 网格所限）；slope-only 各实现均 250–400K 平坦 96.1–96.5% → 窗口受共模失调而非斜率限制，**共模校正应上全局写压轨**（400K 达 −214mV 超 per-cell trim 量程），per-cell trim 留给 D2D；RC 热时钟修正轴（400K 处 τ 比 kT-only 简化短约 2 倍）。**核查修正（2 major 于此项：单位族 b 偏小 4.4% 已改检查点族 V_T=0.022422；实现/n 假设未声明已补带）**。稿件段等编辑窗口：引 Rehm arXiv:2310.18779（弹道乐观端）+ arXiv:2410.16915（权重空间补偿对照）。
- [x] **T2-3 计数式 RC 读出**（2026-07-13 完成，核查修正重跑 3 种子+消融）：`rc_counting_readout.py` 正典工作点（N=100, dt=25ns, S=25）物理系综台架。**边界结论（负向，排除"复用 PBNN 计数器"路线）**：E=96 时诚实单外围系统（递归+读出同用单选通 popcount）MC 1.59 vs 散粒匹配 SAR(b*=5) 2.23，读出能量 24×（平均递增计费 20×；SAR 未计积分器只会更不利于计数）；积分窗规则 N_eff≤1.57 → 分辨率只能来自系综（4^b 律）。**消融**（E∈{16,96,1024} 全确认）：递归通道噪声贡献在估计噪声内（E=96：readout-only 2.15±0.12 vs 两通道 2.27±0.10）→ 系综噪声经**读出通道**限制 MC（均场天花板 4.92±0.03）。19.4fJ 口径=sky130 标准单元电容估计（~2× 不确定度，非 ngspice 实测）。ch5:108 的 arXiv:2601.21807 误引已改正（系综平均读出去噪，非 ADC 分辨率结论）。
- [x] **T3-5 确定性重放列级共仿**（2026-07-13 完成，对抗核查 5 major 全修后重跑）：`eda/hero/replay_column_cosim.py` 64 单元差分列 + met1 位线梯（提取板阻 1.09Ω/段）+ 虚地 TIA（行为级运放——**必需**：列 Norton 阻抗 ~115Ω 会压塌无源 R_TI 感测，bring-up 实测）+ 晶体管级 sky130 StrongARM；600 重放×2 失调变体，确定性验证（两次独立运行数字逐位相同）。**核查揪出致命缺陷并修复**：初版 TIA 未消共模电流（~0.4mA/侧灌过 R_TI）→输出共模塌至 −0.09V、SA 输入对截止、判决实为 µV 泄漏残余符号——加匹配参考列共模消除源（CIM 标准哑列技术）后 SA 满轨再生（|vout| 中位 1.800V）。**三层结果（修复版）**：SA 保真 0.9983（0mV，一例小裕量误判）/0.9467（9.21mV 1σ 失调，真偏置效应）；原始行为一致 513/600——行为 LSB 映射缺**线 IR 斜率压缩**（拟合 0.685；**入库版精确节点解** `nodal_solve_check.py` 匹配 ngspice 至 4.2µV、斜率四位小数一致——旧提交引用的 0.71 来自未入库简化模型，勘误于此；远端 IR 12.7mV≈读半摆幅 25%）；一点阈值重标定复得 462/500（输出节点投影口径，已声明；斜率残差留存）。「读通路压降可忽略」断言升级为实测（ir_drop.py 文档已调和：斜率比较的对象是列 Norton 阻抗 ~100Ω 而非 R_P）。Lilliefors p=0.106/0.755（修正了缺参数拟合校正的 KS p=0.50 虚高）；功效语句入 JSON（0.32% 真失配率下 n=500 有 ≥80% 概率现形）；单实例条件性/独立抽样/禁句措辞均已修。残留：稿件小节（验证章）、可选 256 列扩展、多列实例泛化。
- **旧提交信息勘误（提交不可改，此处留痕）**：3e76ec8 "flat 96.2-96.5%" 实为 96.125–96.525%、"+-215 mV" 实为 −214.2mV；9096c5e 把 19.4fJ 归因 "ngspice" 有误（标准单元电容估计）；59f49c1 "0.3-0.4% agreement" 应表述为 ~1% MC 不确定度内一致；9e36c28 "numpy nodal solve: 0.71" 来自未入库简化模型（忠实精确解=0.6853，见 nodal_solve_check.py），其 "SA faithfulness 1.000/0.947" 系共模塌陷伪影（修复后 0.9983/0.9467）、"KS p=0.50" 缺 Lilliefors 校正（正确 0.106）。
- [x] **MTJ L1 抽象 BEOL 版图**（2026-07-08 完成，全脚本无 GUI）：`gen_2t_cell.py`→`cell2t.gds`，CMOS 部分 DRC 真 0 违例（feol+beol+offgrid + 阳性对照）、PEX 2 FET + ~3.1 fF、设计层 22.7 µm²、黑盒 200/0+201/0、剖面图 `figures/cell2t_cross_section.*`、串扰证书已回灌（δp=1.4e-6 @4.06 µm）。**副产物勘误：SA「DRC 0 违例」系无开关假阴性**（~542 条 PCell 偏格/压线伪影；正文无此宣称，内部记录已改；SA 网格吸附修复随 1.7）。T3-5 的 2T 单元模板就绪。残留：可选 LVS、GDS 渲染图（GUI）
- [x] **L2b 双 CD 候选去留决定**（2026-07-12 按建议执行降格）：不再作候选贡献点、不做新颖性查证；设计窗结论已以一句讨论入正文（ch4 §4.6「需把电学直径缩到约 17 nm 或把界面各向异性修剪约 10%，故超顺磁变体更可能来自近补偿叠层的微调而非几何缩小」+ 图 4.23(a) 承载曲线）。
- [x] 跨仓库口径修正：ch2 D_elec 派生量说明、configs.py「β-IrMn」注释——经用户确认已完成（2026-07-12）

## 阻塞项登记

- A7 需用户提供 IEDM 2024 全文自查页（或授权换锚）。
- 1.7 SA GUI 布线（解锁 1.8–1.10）需 GUI 会话。
- VA 同步窗口等 canonical vgsot-sim 验证通过（`../eda/VA_SYNC_RUNBOOK.md`）。
