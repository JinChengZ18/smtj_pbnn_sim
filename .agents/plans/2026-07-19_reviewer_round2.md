# 第二轮审稿人核查·剩余有价值实验清单（2026-07-19）

> 来源：小型核查工作流 run `wf_a848c567-4f2`（3 读者读现行 ch4/ch5 + 已完成清单 → 3 审稿视角提案 12 条 → 合并去重 7 项）。不含联网新颖性核验（本轮目标=章节完整性/防守面，非发表新颖性）。执行进度就地打勾并回写 `EXECUTION.md`。

# 审稿追问封堵提案·合并去重备忘

12条提案合并为7项（ECC基线三镜头重合、§5.4物理系综复验三镜头重合、多种子误差棒双镜头重合），按（封住追问的价值×可行性÷成本）降序排列。lens溯源标注于各条标题后。

---

## 1. 同一阵列双模式工作点一致性闭环（Δ=4.91上的RC恢复）〔lens：committee〕

- **针对章节**：ch4+ch5（跨章主线）
- **封住的追问**：「图5.10题注断言『同一物理阵列…并列概率位推断与储备池两种时分复用用法』，而§5.4给出『RC最优Δ≈4.1(高原3.5–4.3)，明显低于PBNN写器件Δ=4.91』——同一颗芯片到底能不能跑两种模式？在Δ=4.91上RC掉多少？」
- **做法**：从exp15既有(Δ,dt)记忆容量图切出Δ=4.91行，验证按τ*≈2–3×dt时间尺度匹配律放慢时钟后MC能否恢复到高原值，用exp16口径量化放慢时钟的吞吐/能耗代价；反向的back-hopping论证（Δ=4.1器件在PBNN读窗内p_max过高）以一句话引用exp08既有消融轴，不做新实验。
- **依托设施**：exp15现有run数据切片 + arrhenius.py + exp16能耗口径
- **规模**：S（大部分数据直接切片，新计算量极小）
- **产出形态**：§5.4末或§5.5系统整合处新增一段+脚注；图5.3(c)加注Δ=4.91工作点的dt重匹配恢复值
- **取舍建议**：**必做，排第一**。论文主线就是「同一器件两类应用」，图5.10已写成正文断言而两章最优Δ相差0.8，不闭合此环答辩第一轮即被追问；成本又是全部提案里最低的一档。内部裁剪：反向back-hopping方向压缩为一句引用即可，不值得为它新建实验。

## 2. 比特翻转对照补SECDED-ECC数字基线〔lens：device+ml+committee，三镜头独立提出〕

- **针对章节**：ch4
- **封住的追问**：「比特翻转对比(44 pp优势)中FP-NN为无ECC保护的裸8位编码，实际数字MRAM部署通常配ECC；正文未做带ECC对照，优势幅度对基线设定敏感」
- **做法**：在exp09编码框架里加FP-INT8+SECDED Hamming(72,64)变体，按闭式残余错误率p_resid(p)=P(72位码字内≥2错)折算有效翻转率后重报同一p扫描，附12.5%位开销与解码能量注记。预期诚实结论：p≤1e-3低翻转区ECC完全修复数字基线，p=0.1时码字期望约7错、SECDED崩溃，44pp优势原样保持但适用区间被明确界定。
- **依托设施**：experiments/09_hardware_bitflip.py（翻转注入与编码评估框架现成）+ ppa能量/面积口径
- **规模**：S（闭式折算近零计算成本）
- **产出形态**：表4.5新增FP-INT8+SECDED一行 + 图4.10可选一条曲线 + 解释性脚注（残余率、位开销、适用区间）
- **取舍建议**：**必做**。三个镜头独立命中，「数字MRAM带ECC」几乎是本方向答辩必问；预期结果方向对本文有利（headline处ECC必然失效），做完后对比从「裸基线碾压」升级为「给出ECC失效边界的诚实定界」，反而更强。内部裁剪：采用committee版的闭式折算路线；device版提出的(12,8)逐权重第二档边际价值低，**砍掉**，只保(72,64)共享码字一档即可覆盖追问。

## 3. 列高N扫描：IR斜率压缩的适用边界〔lens：device〕

- **针对章节**：ch4
- **封住的追问**：「4.3节称256×256以下子阵列IR影响可被外围数字阈值吸收、不默认开启，而4.6节64单元列重放即显示0.69斜率压缩、判决一致率仅86%——两处口径的适用边界未在正文内调和」
- **做法**：把nodal_solve_check.py硬编码的N=64参数化为N∈{16,32,64,128,256}，用同一精确线性求解器（N=64处已与ngspice锚定至4.2 µV）扫描斜率压缩、远端IR与判决一致率随列高的变化，产出「无补偿阈值吸收」成立的列高边界，在§4.3与§4.6各加一句交叉引用把适用范围写死。
- **依托设施**：eda/hero/nodal_solve_check.py（纯numpy，分钟级）+ replay_column_cosim.py种子确定性状态再生
- **规模**：S（改一个常数重跑）
- **产出形态**：附录单面板图或表行 + §4.3/§4.6各一句 + 脚注
- **取舍建议**：**必做**。同一篇论文两处给出表面矛盾的IR结论而无边界声明，是器件-电路审稿人最容易抓的内部一致性硬伤；ngspice锚定为外推提供背书，改常数重跑即可。每个N的一点重标定恢复率也保留，成本可忽略。

## 4. 图5.4热时钟数字与标定温度链同步〔lens：device〕

- **针对章节**：ch5
- **封住的追问**：「温轴口径订正（脚注承认高温端τ高估约2倍）后，图5.4正文数字（181→18 ns、漂移2.33/0.17、配方系数2.31）仍按简化口径给出，图与修正口径未同步，定量结论与脚注声明之间存在悬置」
- **做法**：把exp19本地的delta_of_T简化口径替换为thermal_scaling.py的as-built标定链（Bloch M_s + n=2.18经81%补偿放大），重生成图5.4四面板并更新正文三处数字；[^tau_axis_note]保留为试错记录但改写为已同步时态。热时钟配方本身不依赖口径，预期方向不变、数值收紧。
- **依托设施**：src/smtj_pbnn_sim/device/thermal_scaling.py（T2-2已LLG双锚验证至1%以内）+ experiments/19_rc_temperature.py
- **规模**：S（换一个函数重跑）
- **产出形态**：重生成图5.4 + 正文三处数字更新 + 脚注时态改写
- **取舍建议**：**必做**。属一致性收尾而非新知识，但脚注已公开承认口径误差而图未跟进，等于留了一个自证矛盾的靶子；T2-2设施完全现成，不做的机会成本远大于做的成本。执行时注意与§5.4已入的热时钟修正段措辞对齐，避免二次口径漂移。

## 5. 核心精度差的配对多种子误差棒〔lens：ml+committee〕

- **针对章节**：ch4
- **封住的追问**：「精度结论普遍为单实例、无多种子误差棒：0.07 pp(PBNN vs BNN)、0.17 pp(T=4 vs 64)级差异在无方差报告下不具统计说服力；[^eot_audit]自己承认对抗精度随训练实例浮动10–16 pp……但主表未同等处理」
- **做法**：先零成本浮出runs/21_seed_independence既有的四个旗舰数字±std到表4.1/4.2；再把exp07接入8种子harness（exp25b循环为样板），逐种子训PBNN/BNN/FP32、推理端每实例测T=4与T=64，报告三对关键差值的配对均值±std（配对差消掉实例间共同方差）。exp25b先例（±0.22pp）表明种子方差正好在0.07–0.17pp量级，大概率结果是措辞从「重合」改为「配对差X±Ypp、统计不可分辨」——而这恰恰使「1.5pp差距归因于二值容量」的核心归因获得统计支撑。
- **依托设施**：experiments/21_seed_independence.py（已跑通留存）+ experiments/07_baseline_comparison.py + exp25b配对差报告样式
- **规模**：S偏M（真正新计算只有8种子×3模型MNIST-MLP训练，数十分钟量级）
- **产出形态**：表4.1/4.2加±std列或统一脚注（含配对差口径与种子数）；若口径改变同步修两处正文措辞；无需新图
- **取舍建议**：**必做**。「误差棒在哪」是工科答辩标配问题，命中概率接近确定，且exp21已覆盖大半、剩余是整合写作。内部裁剪：时间紧可减至5种子仍够报std；先做零计算的exp21浮出，训练部分可后置。

## 6. §5.4器件优化结论的物理系综复验〔lens：device+ml+committee，三镜头独立提出〕

- **针对章节**：ch5
- **封住的追问**：「5.4全部器件优化结论只在均值场模式下取得，而5.5末自己承认均值场是无穷系综上限；这些设计指导在含散粒噪声的物理系综下是否保持（最优Δ是否漂移）未复验」及「变异有益…强表述超出证据覆盖面」
- **做法**：在mode="stochastic"、E=96（与图5.2f/图5.11同口径）、3种子下重跑两条一维主线：(i)固定dt下MC vs Δ（约6个Δ点），核验最优高原3.5–4.3位置是否漂移；(ii)CV(Δ)有益性在两个锚点（晶圆实测7.7%与30%）是否在读出受限MC下存活。不重做二维等高线。预期MC整体被系综噪声压低但极值位置由器件物理决定而不漂——若保持则脚注一句收口，若漂移则升格为面板且本身就是新发现。顺带在同批run里报告NARMA-10/MG的物理模式数字（与第7项合并计算）。
- **依托设施**：reservoir/node.py双模式（纯配置切换，零新代码）+ exp15/exp17扫描骨架 + T2-3已验证的系综机制
- **规模**：M（E=96×Δ网格×3种子，一维化后可控；本清单唯一M档）
- **产出形态**：图5.3(c)与图5.5(a)各叠一条物理模式误差带（或合成小图入附录）+ §5.4口径句改写为「均值场定位极值、物理模式复验位置保持」
- **取舍建议**：**必做，且是唯一可能产出新知识的一项**。§5.4被正文定位为本章相对既有自旋电子RC工作的主要增量，却建立在自己随后降级的口径上，属贡献点自身的逻辑硬伤，不复验则该贡献在答辩中站不住。排序靠后仅因M档成本除低了比值，不代表优先级低——它是最该**先开跑**的（见执行顺序）。内部裁剪：若时间极紧，砍到committee的S版（单条Δ曲线+两个D2D锚点），覆盖面略窄但足以答辩。

## 7. 基准数字的模式诚实化（MG与NARMA-10）〔lens：ml〕

- **针对章节**：ch5
- **封住的追问**：「NARMA-10 NRMSE≈0.79接近平凡基线(NRMSE=1即预测均值)，正文未与软件ESN典型值对比、也未承认该数值意味着物理模式下几乎无预测能力」与「Mackey-Glass NRMSE 0.006未标明是均值场还是物理采样模式取得」
- **做法**：在图注/脚注明示0.006为均值场上限、0.79为E=96物理值，补软件ESN文献参照值区间；物理模式数字搭第6项的stochastic run顺带产出，不单独起跑。
- **依托设施**：experiments/18_rc_benchmarks.py + reservoir/tasks.py + 第6项的同批stochastic配置
- **规模**：S（计算并入第6项后趋近零，剩写作）
- **产出形态**：口径脚注（[^ipc_proxy]同风格）+ 图5.6图注补模式标注
- **取舍建议**：**做脚注版，砍图和E扫描**。原提案的E∈{16,96,384}三档扫描与E→∞外推边际价值低——1/√E收敛路径图5.2(f)已建立，重复论证一遍是冗余；可选附录小图也**放弃**。真正必要的只是给两个亮点数字标模式、给0.79一个文献参照系，这是「亮点数字诚实性」问题，一条脚注即可封口，且负结果降格已有exp27/T2-3先例、表述路径现成。

---

## 放弃项与内部裁剪汇总

七个合并方向**无整项建议放弃**——原始12条里边际价值低的重复项已在合并中吸收，留下的每项都对应一个答辩高概率追问且成本在S/M档。但各项内部有明确该砍的部分：第2项砍(12,8)逐权重ECC档；第1项反向back-hopping压缩为一句引用；第6项砍二维等高线、保两条一维曲线；第7项砍E扫描与附录图、只留脚注。若被迫整体砍一项，砍第7项的独立实验部分（其计算已可完全并入第6项，纯写作补丁保留即可）。

## 建议执行顺序

第一步先把第6项的stochastic扫描**后台开跑**（唯一M档、墙钟最长，且第7项的数字搭它的车）；等待期间完成四个S档速赢：第1项（切片exp15现有数据）、第3项（改常数重跑求解器）、第4项（换函数重生图5.4）、第2项（闭式ECC折算），这四项均为分钟级计算+写作，可在一两个工作日内全部落盘；随后做第5项（先零成本浮出exp21数字，再跑8种子×3模型训练）；最后待第6项run完成，据结果决定§5.4是脚注收口还是升格面板，并同批写入第7项的模式标注脚注。全部完成后按既有管线做一轮对抗式审校（重点核对§5.4热时钟措辞、§4.3/§4.6交叉引用口径、表4.1/4.2/4.5的±与新行数字可复现性），再统一build_docx同步。
